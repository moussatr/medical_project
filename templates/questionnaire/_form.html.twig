<link rel="stylesheet" href="{{ asset('css/style.css') }}">
{{ form_start(form) }}
<div class="row">
    <div class="col">{{ form_row(form.Prenom) }} </div>
     <div class="col">{{ form_row(form.Nom) }} </div>
 </div>
 {{ form_rest(form) }}
    <button class=" btn btn-primary p-1" >{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}